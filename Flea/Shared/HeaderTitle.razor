@using Flea.Utility
@code {

    [Parameter, RequiredParameter]
    public string Value {
        set
        {
            if (value == null!)
                throw new ArgumentNullException(nameof(Value), "Parameter HeaderTitle.Value is not allowed to be null.");
            MainLayout.HeaderTitle = value;
        } 
    }

}